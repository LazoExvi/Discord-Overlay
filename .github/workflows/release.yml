name: Release installer

# Pushing a tag such as v1.0.1 (matching discord_overlay.__version__) builds the
# Windows installer and portable zip and publishes them as a GitHub Release.
# "Run workflow" from the Actions tab builds the current commit as a pre-release.

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Read version
        id: version
        shell: pwsh
        run: |
          $match = Select-String -Path discord_overlay/__init__.py -Pattern '__version__ = "([0-9.]+)"'
          $version = $match.Matches[0].Groups[1].Value
          "version=$version" >> $env:GITHUB_OUTPUT
          if ("${{ github.ref_type }}" -eq "tag" -and "${{ github.ref_name }}" -ne "v$version") {
            throw "Tag ${{ github.ref_name }} does not match __version__ $version"
          }

      - name: Build folder and installer
        shell: pwsh
        run: .\scripts\build_windows.ps1 -Variant directml -Installer

      - name: Create portable zip
        shell: pwsh
        run: Compress-Archive -Path dist\DiscordOverlay -DestinationPath "DiscordOverlay-${{ steps.version.outputs.version }}-portable.zip"

      - name: Publish release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $version = "${{ steps.version.outputs.version }}"
          $installer = "packaging/output/DiscordOverlay-Setup-$version.exe"
          $zip = "DiscordOverlay-$version-portable.zip"
          $notes = @"
          **Install:** download ``DiscordOverlay-Setup-$version.exe`` and run it. No Python needed.
          Windows may show "Windows protected your PC" because the installer is not code-signed:
          click **More info**, then **Run anyway**.

          The portable zip is the same program without an installer: unzip anywhere and run ``DiscordOverlay.exe``.

          GPU acceleration uses DirectML and works on NVIDIA, AMD, and Intel graphics; it falls back to CPU automatically.
          "@
          if ("${{ github.ref_type }}" -eq "tag") {
            gh release create "${{ github.ref_name }}" $installer $zip --title "Discord Overlay $version" --notes $notes
          } else {
            $tag = "build-${{ github.run_number }}"
            gh release create $tag $installer $zip --title "Discord Overlay $version (build ${{ github.run_number }})" --notes $notes --prerelease --target ${{ github.sha }}
          }
